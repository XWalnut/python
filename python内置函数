# 如何查询数据类型
a = int(input())
print(type(a))

# map函数
num = input().split(' ')
num_list = list(map(int,num))# map 将num与int映射，list将映射结果存在列表中
print(num_list)

# join函数,用-连接字符串 a-b-c
'-'.join(str1)

# round函数 如果位数不够，就不操作
round(num,digit) # digit = 0 四舍五入到整数，digit > 0 四舍五入到小数点后digit位，digit < 0 小数点左侧进行四舍五入
