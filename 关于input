# input输入的默认是字符串
# 将字符串分割成列表，以空格为分隔符
str_list = input().split(' ')

# 将字符串转换成日期，需要用到日期库函数
str_1 = input()
str_2 = input()
str_dateTime = datetime.datetime.strptime(str_1,'%Y%m%d %H:%M:%S')
end_dateTime = datetime.datetime.strptime(str_2,'%Y%m%d %H:%M:%S')
# 日期相减 .days .hours .seconds
(end_dateTime - str_dateTime).days #转换成天数
